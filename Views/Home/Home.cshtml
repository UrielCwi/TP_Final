@{
    ViewData["Title"] = "Home";
}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>Card</title>
    <html lang="en">
</head>

<body>
    <p>Bienvenido de vuelta, @ViewBag.Usuario.Nombre</p>
        <div class="buttonAdd">
            <button class="complete-button" onclick="NuevaTarea()">Agregar Tarea</button>
        </div>
        @foreach(var item in ViewBag.Tareas){
            <div class="card">
            <div class="header">
                <h2>Título</h2>
                <div class="tag">Colegio</div>
            </div>
            <p>@ViewBag.item.Descripcion</p>
            <button class="complete-button" onclick="markAsCompleted()">Marcar como completado</button>
        </div>
        }
    <script src="script.js"></script>
</body>
</html>