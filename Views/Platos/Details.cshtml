@{
    ViewData["Title"] = "Agregar Plato";
}

<h2>@ViewData["Title"]</h2>

<h2>Gestión de Platos</h2>

<p>
    <a asp-action="Create" class="btn btn-primary">Agregar Nuevo Plato</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Categoría</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.nombre</td>
            <td>@item.idCategoria</td>
            <td>
                <a asp-action="Editar" asp-route-id="@item.id" class="btn btn-secondary">Editar</a> |
                <a asp-action="Eliminar" asp-route-id="@item.id" class="btn btn-danger">Eliminar</a>
            </td>
        </tr>
    }
    </tbody>
</table>